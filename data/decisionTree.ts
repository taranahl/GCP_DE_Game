export interface DecisionCard {
  id: string;
  category: string;
  need: string;
  tool: string;
  why: string;
  whenNotToUse: string;
  relatedTerms: string[];
}

const decisionCards: DecisionCard[] = [
  // Storage
  {
    id: "cheap-file-storage",
    category: "storage",
    need: "Cheap, durable raw file storage",
    tool: "Cloud Storage",
    why: "Cloud Storage is Google's object store — like a filing cabinet in the cloud. It stores any file type cheaply and durably. It is the standard landing zone for raw data before it is processed or loaded into BigQuery.",
    whenNotToUse: "Do not use Cloud Storage when you need to run SQL queries directly over the data — Cloud Storage has no query engine. Use BigQuery or External Tables for that.",
    relatedTerms: ["cloud-storage", "data-lake", "ingestion"],
  },
  {
    id: "sql-analytics",
    category: "storage",
    need: "SQL analytics over large datasets",
    tool: "BigQuery",
    why: "BigQuery is Google's serverless data warehouse. It runs SQL over petabytes in seconds. You pay per query (or per slot reservation). It is the standard analytics engine for GCP data pipelines.",
    whenNotToUse: "Do not use BigQuery as an operational database for high-volume point lookups or transactional writes. It is optimised for analytical reads over large volumes, not individual row updates.",
    relatedTerms: ["bigquery", "data-warehouse", "partitioning", "clustering"],
  },
  {
    id: "query-files-without-loading",
    category: "storage",
    need: "Query files in Cloud Storage without loading them into BigQuery",
    tool: "External Tables or BigLake",
    why: "External Tables let you run SQL against files in Cloud Storage directly from BigQuery without copying them. BigLake adds column-level security and unified governance on top of the same pattern.",
    whenNotToUse: "External table queries are slower than native BigQuery tables and do not support DML (UPDATE, DELETE). For frequently queried data, load it into native BigQuery storage instead.",
    relatedTerms: ["external-tables", "biglake", "federated-query", "data-lake"],
  },
  {
    id: "governed-lakehouse",
    category: "storage",
    need: "Governed lakehouse: files in Cloud Storage + SQL access + security",
    tool: "BigLake + Dataplex",
    why: "BigLake provides governed external tables — column-level policy tags apply even to files in Cloud Storage. Dataplex adds catalog, lineage, and data quality rules on top. Together they build a lakehouse without copying data into BigQuery native storage.",
    whenNotToUse: "Do not use this for simple file storage. The lakehouse pattern adds governance overhead. If you just need cheap file storage, use Cloud Storage. If you just need SQL analytics, use native BigQuery.",
    relatedTerms: ["biglake", "dataplex", "lakehouse", "external-tables", "data-lineage"],
  },
  // Ingestion
  {
    id: "batch-file-load",
    category: "ingestion",
    need: "Simple batch file loading into BigQuery",
    tool: "bq load / LOAD DATA",
    why: "bq load and BigQuery's LOAD DATA SQL statement are the simplest way to load files from Cloud Storage into BigQuery. No pipeline infrastructure needed — great for one-time loads or simple scheduled loads.",
    whenNotToUse: "Do not use bq load for real-time or near-real-time data. It is a batch operation. For streaming, use Pub/Sub → Dataflow.",
    relatedTerms: ["bq-load", "batch-ingestion", "cloud-storage", "bigquery"],
  },
  {
    id: "saas-transfer",
    category: "ingestion",
    need: "Scheduled transfer from a supported SaaS source (Google Ads, YouTube, Salesforce) into BigQuery",
    tool: "BigQuery Data Transfer Service",
    why: "BigQuery Data Transfer Service has native, managed connectors for popular SaaS sources. Configure it once and data loads on a schedule — no custom pipeline code needed.",
    whenNotToUse: "Do not use BigQuery Data Transfer Service for sources it does not support natively. For custom sources, build a pipeline with Dataflow or Pub/Sub.",
    relatedTerms: ["bigquery-data-transfer-service", "batch-ingestion", "saas"],
  },
  {
    id: "real-time-events",
    category: "ingestion",
    need: "Real-time event ingestion from an app, website, or IoT device",
    tool: "Pub/Sub",
    why: "Pub/Sub is the front door for real-time events. It decouples the event producer (your app) from the event consumer (Dataflow, BigQuery). It buffers messages so neither side depends on the other's availability.",
    whenNotToUse: "Do not use Pub/Sub for CDC from a database — use Datastream instead. Pub/Sub handles app-level events, not database replication logs.",
    relatedTerms: ["pubsub", "streaming-ingestion", "dataflow"],
  },
  {
    id: "streaming-transformation",
    category: "ingestion",
    need: "Real-time stream processing: clean, deduplicate, window, enrich events",
    tool: "Dataflow",
    why: "Dataflow (Apache Beam) is Google's managed streaming processor. It reads from Pub/Sub, applies transformations (deduplication, windowing, enrichment), and writes to BigQuery or Cloud Storage. Serverless — no cluster to manage.",
    whenNotToUse: "Do not use Dataflow when you have existing PySpark code — use Dataproc instead. Dataflow requires Apache Beam, which means rewriting Spark pipelines.",
    relatedTerms: ["dataflow", "apache-beam", "streaming-processing", "windowing", "deduplication"],
  },
  {
    id: "cdc-from-database",
    category: "ingestion",
    need: "CDC (Change Data Capture) from a relational database — capture inserts, updates, deletes",
    tool: "Datastream",
    why: "Datastream reads the replication log of MySQL, PostgreSQL, or Oracle databases and streams every change into BigQuery or Cloud Storage. It captures updates and deletes — something a nightly export cannot do.",
    whenNotToUse: "Do not use Datastream for app-level event ingestion. Datastream needs access to the database replication log, not application events. For app events, use Pub/Sub.",
    relatedTerms: ["datastream", "change-data-capture", "streaming-ingestion", "bigquery"],
  },
  {
    id: "file-migration",
    category: "ingestion",
    need: "Bulk file migration from AWS S3, Azure, or on-premises NAS to Cloud Storage",
    tool: "Storage Transfer Service",
    why: "Storage Transfer Service handles large-scale file migrations between cloud storage systems. It retries failed transfers, supports scheduling for ongoing sync, and has no per-GB compute cost to manage.",
    whenNotToUse: "Do not use Storage Transfer Service for CDC from a database or for real-time event streaming. It moves files, not database records or event streams.",
    relatedTerms: ["storage-transfer-service", "cloud-storage", "data-lake", "batch-ingestion"],
  },
  // Processing
  {
    id: "spark-hadoop",
    category: "processing",
    need: "Run existing PySpark or Hadoop jobs on GCP",
    tool: "Dataproc",
    why: "Dataproc is Google's managed Spark and Hadoop service. Existing Spark code runs with minimal changes. It is the lift-and-shift option for teams migrating from on-premises Spark clusters.",
    whenNotToUse: "Do not use Dataproc when you are building a new pipeline from scratch with no existing Spark code. Dataflow (serverless) or Dataform (SQL) are simpler and cheaper for new builds.",
    relatedTerms: ["dataproc", "spark", "pyspark", "hadoop", "batch-processing"],
  },
  {
    id: "visual-etl",
    category: "processing",
    need: "Visual, low-code ETL pipeline for teams without deep coding skills",
    tool: "Data Fusion",
    why: "Data Fusion provides a drag-and-drop ETL pipeline builder. Connects to many sources. Runs on Dataproc under the hood. Good for non-engineering teams who need to build pipelines without writing code.",
    whenNotToUse: "Do not use Data Fusion for high-throughput streaming pipelines. It is designed for batch ETL. For streaming, use Dataflow.",
    relatedTerms: ["data-fusion", "etl", "batch-processing"],
  },
  {
    id: "sql-transformation",
    category: "processing",
    need: "SQL-based transformation with dependency management and data quality tests",
    tool: "Dataform",
    why: "Dataform is a SQL-based transformation tool that runs inside BigQuery. It manages table dependencies (table A must complete before table B), runs data quality assertions, and version-controls all SQL in Git.",
    whenNotToUse: "Do not use Dataform for non-SQL transformations. If your pipeline needs Python or Java code, use Dataflow instead.",
    relatedTerms: ["dataform", "elt", "bigquery", "data-quality", "dag"],
  },
  {
    id: "simple-recurring-sql",
    category: "processing",
    need: "Simple recurring SQL query that runs on a schedule (no dependency management needed)",
    tool: "BigQuery Scheduled Queries",
    why: "BigQuery Scheduled Queries runs a SQL statement on a timer. Great for daily summary tables, simple aggregations, or refresh queries. No pipeline infrastructure needed.",
    whenNotToUse: "Do not use Scheduled Queries when you have multiple dependent steps (A must run before B must run before C). Use Dataform for dependency management.",
    relatedTerms: ["bigquery-scheduled-queries", "batch-processing", "bigquery"],
  },
  // Orchestration
  {
    id: "workflow-orchestration",
    category: "orchestration",
    need: "Complex multi-step workflow orchestration with retries, branching, SLAs, and backfill",
    tool: "Cloud Composer (Airflow)",
    why: "Cloud Composer is managed Apache Airflow. It orchestrates complex DAGs with many steps, handles retries and failure alerting, and supports backfilling historical runs. The standard choice for production data pipeline orchestration.",
    whenNotToUse: "Do not use Composer for simple API chaining or single-step triggers — it is expensive and heavyweight. Use Workflows for API chains or Cloud Scheduler for simple cron triggers.",
    relatedTerms: ["cloud-composer", "apache-airflow", "dag", "orchestration", "backfill"],
  },
  {
    id: "api-orchestration",
    category: "orchestration",
    need: "Lightweight API and service orchestration — chain Google Cloud APIs without a full DAG engine",
    tool: "Workflows",
    why: "Workflows chains Google Cloud API calls with conditional logic and error handling. Great for triggering a Dataflow job, then loading results to BigQuery, then sending a notification — without the overhead of Composer.",
    whenNotToUse: "Do not use Workflows for pipelines with many parallel steps, complex branching, or backfill requirements. Use Cloud Composer for those.",
    relatedTerms: ["workflows", "orchestration"],
  },
  {
    id: "cron-trigger",
    category: "orchestration",
    need: "Trigger a job, HTTP endpoint, or Cloud Function on a cron schedule",
    tool: "Cloud Scheduler",
    why: "Cloud Scheduler is GCP's cron service. It sends HTTP requests or Pub/Sub messages on a schedule. Use it to trigger Workflows, Cloud Functions, or Dataflow jobs at set intervals.",
    whenNotToUse: "Do not use Cloud Scheduler as a DAG orchestrator — it just fires a trigger. It does not track job completion, retry on failure, or manage dependencies between steps.",
    relatedTerms: ["cloud-scheduler", "cron", "orchestration"],
  },
  // Serving
  {
    id: "dashboards",
    category: "serving",
    need: "Dashboards and data visualisation for business stakeholders",
    tool: "Looker Studio (free) or Looker (enterprise)",
    why: "Looker Studio is free, connects directly to BigQuery, and is easy for non-engineers to use. Looker is the enterprise product with more governance, semantic modelling, and embed capabilities. Start with Looker Studio.",
    whenNotToUse: "Do not use Looker Studio for pixel-perfect print reports or as an operational database. It is a BI tool — use it on top of well-modelled BigQuery data.",
    relatedTerms: ["looker-studio", "looker", "bigquery"],
  },
  {
    id: "ml-in-bigquery",
    category: "serving",
    need: "Train and deploy simple ML models using SQL inside BigQuery",
    tool: "BigQuery ML",
    why: "BigQuery ML lets you create, train, and evaluate ML models using SQL. No Python or separate ML infrastructure needed. Great for linear regression, logistic regression, and time-series forecasting directly on your warehouse data.",
    whenNotToUse: "Do not use BigQuery ML for complex deep learning, custom model architectures, or models that need GPU training. Use Vertex AI for those.",
    relatedTerms: ["bigquery-ml", "vertex-ai", "bigquery"],
  },
  {
    id: "ml-platform",
    category: "serving",
    need: "End-to-end ML platform: custom model training, experiment tracking, model deployment",
    tool: "Vertex AI",
    why: "Vertex AI is Google's full ML platform. It handles custom model training (TensorFlow, PyTorch, Scikit-learn), AutoML, experiment tracking, model registry, and online/batch prediction serving.",
    whenNotToUse: "Do not use Vertex AI for simple SQL-based models — BigQuery ML is faster and cheaper. Vertex AI is the right choice when you need custom model code or GPU training.",
    relatedTerms: ["vertex-ai", "bigquery-ml"],
  },
  // Governance
  {
    id: "data-catalog-lineage",
    category: "governance",
    need: "Data catalog, lineage tracking, and data quality across all assets",
    tool: "Dataplex / Knowledge Catalog",
    why: "Dataplex catalogs all BigQuery tables and Cloud Storage assets, tracks data lineage (where data came from and where it flows), and runs data quality rules. It is the single governance control plane for your GCP data platform.",
    whenNotToUse: "Dataplex is not a replacement for PII detection — use Sensitive Data Protection for scanning and classifying sensitive data. Dataplex handles cataloging and lineage, not PII masking.",
    relatedTerms: ["dataplex", "metadata", "data-lineage", "data-quality", "data-discovery"],
  },
  {
    id: "identity-permissions",
    category: "governance",
    need: "Identity, permissions, and access control across all GCP resources",
    tool: "IAM (Identity and Access Management)",
    why: "IAM is how you control who can do what on GCP. Every resource — BigQuery dataset, Cloud Storage bucket, Dataflow job — is governed by IAM roles. Apply the principle of least privilege: analysts get Data Viewer, not Editor.",
    whenNotToUse: "IAM alone is not sufficient for column-level security inside BigQuery tables. Use Policy Tags for column-level access control on top of IAM.",
    relatedTerms: ["iam", "policy-tags", "bigquery"],
  },
  {
    id: "column-security",
    category: "governance",
    need: "Restrict access to specific sensitive columns in BigQuery (column-level security)",
    tool: "Policy Tags",
    why: "Policy Tags attach to BigQuery columns. Users without the Fine-grained Reader role see NULL instead of real values when querying tagged columns. This enforces column-level security without moving or masking data.",
    whenNotToUse: "Policy Tags apply only to native BigQuery tables — not to External Tables or BigLake tables directly. Plan your table architecture accordingly.",
    relatedTerms: ["policy-tags", "column-level-security", "iam", "bigquery", "pii"],
  },
  {
    id: "pii-detection",
    category: "governance",
    need: "Detect, classify, and optionally mask PII across BigQuery tables and Cloud Storage files",
    tool: "Sensitive Data Protection (Cloud DLP)",
    why: "Sensitive Data Protection scans data for PII — emails, SSNs, phone numbers, credit card numbers, and more. It classifies findings and can de-identify (mask, redact, tokenize) sensitive values in place or in a copy.",
    whenNotToUse: "Sensitive Data Protection is not a replacement for access controls. After identifying PII, you still need Policy Tags and IAM to restrict who can access it.",
    relatedTerms: ["sensitive-data-protection", "pii", "policy-tags", "dataplex"],
  },
  // Monitoring
  {
    id: "pipeline-logs",
    category: "monitoring",
    need: "Capture and query logs from pipelines, services, and GCP resources",
    tool: "Cloud Logging",
    why: "Cloud Logging captures logs from Dataflow, BigQuery, Pub/Sub, and all other GCP services. Query logs via Log Explorer or export to BigQuery for analysis.",
    whenNotToUse: "Cloud Logging is for logs — not for metrics and alerting. Use Cloud Monitoring for setting alert policies on pipeline health and custom metrics.",
    relatedTerms: ["cloud-logging", "sla", "slo"],
  },
  {
    id: "alerts-metrics",
    category: "monitoring",
    need: "Set alerts on pipeline health, custom metrics, and infrastructure issues",
    tool: "Cloud Monitoring",
    why: "Cloud Monitoring collects metrics from all GCP services and lets you set alert policies. When a Dataflow job falls behind, a Pub/Sub backlog grows, or a pipeline fails — Cloud Monitoring fires the alert.",
    whenNotToUse: "Cloud Monitoring does not replace application-level logging. Use Cloud Logging to capture structured error logs; use Cloud Monitoring to track numeric metrics and trigger alerts.",
    relatedTerms: ["cloud-monitoring", "cloud-logging", "sla", "slo", "error-reporting"],
  },
  {
    id: "ai-agent-data",
    category: "agents",
    need: "AI agent that queries data, explains metrics, and answers business questions over your GCP data",
    tool: "Google ADK + Agent Platform",
    why: "Google ADK (Agent Development Kit) lets you build agents that call BigQuery, read Cloud Storage, and interpret results. An agent can answer 'Why did revenue drop on Tuesday?' by querying data, analysing trends, and providing a natural language explanation.",
    whenNotToUse: "Do not use ADK when a simple dashboard or scheduled SQL report would answer the question. Agents add complexity — only use them when the question requires dynamic reasoning over data, not just reading a number.",
    relatedTerms: ["adk", "agent-platform", "tool-calling", "vertex-ai"],
  },
];

export default decisionCards;
